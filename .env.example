# PostgreSQL
DB_HOST=postgres
DB_PORT=5432
DB_NAME=relativa
DB_USER=relativa
DB_PASS=relativa

# pgAdmin
PGADMIN_PORT=5050
PGADMIN_DEFAULT_EMAIL=admin@relativa.com
PGADMIN_DEFAULT_PASSWORD=admin123

# SMTP — leave blank to use MailHog (local dev), fill in for real email delivery
SMTP_HOST=mailhog
SMTP_PORT=1025
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM_ADDRESS=noreply@relativa.com
SMTP_FROM_NAME=Relativa
SMTP_USE_SSL=false

# App
APP_FRONTEND_BASE_URL=http://localhost:3000

# Frontend
CLIENT_PORT=3000
VITE_GATEWAY_URL=http://localhost:8080

# Gateway CORS
CORS_ORIGIN_1=http://localhost:5173
CORS_ORIGIN_2=http://localhost:3000
CORS_ALLOW_ANY_ORIGIN_FOR_DEV=false

# JWT (must match between Relativa.Authentication and Relativa.Gateway)
JWT_SECRET=super-secret-key-that-should-be-at-least-32-chars!
JWT_ISSUER=relativa-auth
JWT_AUDIENCE=relativa
